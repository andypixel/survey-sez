{
  "name": "survey-sez",
  "version": "1.0.0",
  "description": "Real-time multiplayer game prototype",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npm run init-data && npm run kill-port && sleep 1 && nodemon --ignore data/ server.js",
    "dev-all": "npm run init-data && npm run kill-port && sleep 1 && concurrently \"nodemon --ignore data/ server.js\" \"cd client && npm start\"",
    "debug": "npm run init-data && npm run kill-port && sleep 1 && REACT_APP_DEBUG_MODE=true concurrently \"nodemon --ignore data/ server.js\" \"cd client && npm start\"",
    "prod": "npm run init-data && npm run kill-port && sleep 1 && concurrently \"nodemon --ignore data/ server.js\" \"cd client && REACT_APP_DEBUG_MODE=false npm start\"",
    "kill-port": "./kill-server.sh",
    "build": "cd client && npm install && npm run build",
    "heroku-postbuild": "npm run build",
    "init-data": "node scripts/init-dev-data.js",
    "reset-data": "rm -rf data/*.json && npm run init-data",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "redis": "^4.6.0",
    "socket.io": "^4.7.2",
    "winston": "^3.11.0",
    "winston-daily-rotate-file": "^4.7.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.2.0",
    "nodemon": "^3.0.1",
    "socket.io-client": "^4.8.3",
    "supertest": "^7.1.4"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
